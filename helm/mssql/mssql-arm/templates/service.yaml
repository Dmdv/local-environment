apiVersion: v1
kind: Service
metadata:
  name: {{ include "mssql-arm.fullname" . }}
  labels:
    {{- include "mssql-arm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mssql-arm.selectorLabels" . | nindent 4 }}
